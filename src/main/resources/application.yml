spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jwtsecurity
    username: root
    password: My_password2004
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
  servlet:
    multipart:
      max-file-size: -1
      max-request-size: -1
